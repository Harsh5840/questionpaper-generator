services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: qpg_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - qpg_postgres:/var/lib/postgresql/data

volumes:
  qpg_postgres:
